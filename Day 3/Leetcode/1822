class Solution {
    public int arraySign(int[] nums) {
        int sign_ctr=0;
        for(int i : nums){
            if(i==0)  return 0;
            if(i<0) sign_ctr++;
        }
        if(sign_ctr%2==0) return 1;
        return -1;
    }
}
